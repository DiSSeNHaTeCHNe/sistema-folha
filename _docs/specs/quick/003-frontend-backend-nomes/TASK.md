# Quick Task 003: Renomear front/back → frontend/backend

**Date:** 2026-06-20
**Status:** Done

## Description

Ajustar nomes das pastas principais para `frontend/` e `backend/` (em vez de `front/` e `back/`).

## Files Changed

- `front/` → `frontend/` (rename)
- `back/` → `backend/` (rename)
- `Dockerfile`, `.gitignore`, `README.md`, `diversos/README.md`
- `_docs/specs/*.md` — paths atualizados

## Verification

- [x] `frontend/` e `backend/` existem na raiz
- [x] `mvn compile` OK em `backend/`
- [x] Dockerfile referencia `frontend/` e `backend/`
- [x] `.gitignore` não ignora `frontend/` (legado `front/`/`back/` ignorados)

## Commit

Pending — user did not request commit.
